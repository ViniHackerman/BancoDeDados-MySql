<h2>Cadastrar Livros</h2>

<form action="/edit/save" method="POST">
<input type="hidden" value"{{book.id}}" name"id">
    <div>
        <label for="title">Título: </label>
        <input type="text" name="title" placeholder="Digite o Título" id="title" value="{{book.title}}">
    </div>

    <div>
        <label for="pageqty">Numero de Pagina:</label>
        <input type="number" name="pageqty" placeholder="Quantas páginas o livro possui?" id="pageqty" value="{{book.pageqty}}">
    </div>

    <button type="submit">Enviar</button>
</form>